import { ReactNode } from "react";

type Props = {
  children: ReactNode;
};

export default function Header({ children }: Props) {
  return <header className="relative bg-white">{children}</header>;
}
